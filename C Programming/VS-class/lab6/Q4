#include <stdio.h>
int main() {
int n, i;
unsigned long long factorial = 1;

printf("enter a positive integer: ");
scanf("%d", &n);
for(i = 1; i <= n; i++) {

    factorial *= i;

}
printf("factorial of %d = %llu", n, factorial);
return 0;

}